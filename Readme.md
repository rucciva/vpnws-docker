# Dockerozed Websocket-VPN

Based on <https://github.com/unbit/vpn-ws/> . Also includes ISC-DHCP server container and Nginx reverse proxy container.

To start, just run:

``` bash
docker-compose up -d
```